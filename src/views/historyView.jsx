export function HistoryView(props) {
    // console.log("HistoryView props:", props.model.history);
    return "HistoryView stub: " + props.model.history.length + " past readings" + JSON.stringify(props.model.history);
}